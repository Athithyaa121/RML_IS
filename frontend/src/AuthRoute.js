// import React from 'react';
// import { Route, Redirect } from 'react-router-dom';

// const AuthRoute = ({ component: Component, isAdmin, ...rest }) => {
//   return (
//     <Route
//       {...rest}
//       render={(props) => {
//         return isAdmin ? (
//           <Component {...props} />
//         ) : (
//           <Redirect to="/" /> // Change '/home' to the route you want to redirect unauthorized users
//         );
//       }}
//     />
//   );
// };

// export default AuthRoute;
